<콘다 및 파이썬 패키지 업데이트>

콘다 자체 업데이트
#conda update -n base conda

설치된 파이썬 패키지 모두 최신 버전으로 업데이트
#conda update --all
------------------------------------------------------------------------------------------------------------
<tensorflow 설치>

pip 업그레이드하기
#python -m pip install --upgrade pip

conda 환경 만들기
#conda create -n tensorflow python=3.5

tensorflow설치
#activate tensorflow

(tensorflow)>pip install tensorflow #설치완료
------------------------------------------------------------------------------------------------------------
<tensorflow 테스트>

>>>import tensorflow as tf
>>>hello=tf.constant('Hello,Tensorflow!')
>>>sess=tf.Session()
>>>print(sess.run(hello))
Hello,Tensorflow!

>>>a=tf.constant(10)
>>>b=tf.constant(32)
>>>print(sess.run(a+b)
42

------------------------------------------------------------------------------------------------------------
<만약 tensorflow 설치가 안된다면>

pip install tensorflow
or 만약 AVX를 지원하지 않는 CPU를 사용한다면 1.5version 설치 (pip install tensorflow==1.5.0)
